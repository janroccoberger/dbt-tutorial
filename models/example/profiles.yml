jaffle_shop: # this needs to match the profile in your dbt_project.yml file
    target: dev
    outputs:
        dev:
            type: bigquery
            method: oauth
            keyfile: /home/jan_berger/dbt-best-practices-dev-f87c18e0a3e3.json # replace this with the full path to your keyfile
            project: dbt-best-practices-dev # Replace this with your project id
            dataset: dbt-best-practices-dev.jaffle_shop # Replace this with dbt_your_name, e.g. dbt_bilbo
            threads: 1
            timeout_seconds: 300
            location: EU
            priority: interactive
            